/* contrib/meta_sync/meta_sync--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION
\echo Use "CREATE EXTENSION meta_sync" to load this file. \quit

-- Register the function.
